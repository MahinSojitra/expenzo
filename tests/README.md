# Test suite

The application is structured so repository/service methods can be covered with PHPUnit. Add integration tests against a dedicated test database for authentication, expense transactions, authorization and balance calculations before production rollout.
