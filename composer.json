{
    "name": "expense-tracker/pure-php",
    "description": "Enterprise-oriented expense tracker built with pure PHP and MySQL",
    "type": "project",
    "require": {
        "php": ">=8.2",
        "ext-pdo": ">=8.0",
        "ext-json": ">=8.0",
        "ext-fileinfo": ">=8.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "app/"
        }
    },
    "scripts": {
        "test": "phpunit"
    }
}